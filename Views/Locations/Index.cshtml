@using WarehouseMvc.Models
@model IEnumerable<WarehouseMvc.Models.Location>

@{
    ViewData["Title"] = "Locations";

    var totalLocations = Model?.Count() ?? 0;
}

<div class="container mt-4">

    <!-- Header + action button -->
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h2 class="fw-semibold mb-1">Locations</h2>
            <p class="text-muted mb-0">
                Define and manage the physical storage locations in your warehouse.
            </p>
        </div>

        <a asp-action="Create" class="btn btn-primary">
            <i class="bi bi-plus-lg me-1"></i> Add Location
        </a>
    </div>

    <!-- Quick stats -->
    <div class="row g-3 mb-3">
        <div class="col-md-4">
            <div class="border rounded-3 p-3 bg-white">
                <div class="text-muted small text-uppercase">Total Locations</div>
                <div class="h4 mb-0">@totalLocations</div>
            </div>
        </div>
    </div>

    <!-- Table card -->
    <div class="card shadow-sm">
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover mb-0 align-middle">
                    <thead class="table-light">
                        <tr>
                            <th>Name</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Any())
                        {
                            foreach (var location in Model.OrderBy(l => l.Name))
                            {
                                <tr>
                                    <td>@location.Name</td>

                                    <td class="text-end">
                                        <a asp-action="Details" asp-route-id="@location.Id"
                                           class="btn btn-sm btn-outline-secondary me-1">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@location.Id"
                                           class="btn btn-sm btn-outline-primary me-1">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@location.Id"
                                           class="btn btn-sm btn-outline-danger">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="2" class="text-center text-muted py-4">
                                    No locations have been created yet. Click
                                    <strong>Add Location</strong> to set up your first storage area.
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
